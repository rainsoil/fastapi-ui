let baseUrl = import.meta.env.VITE_APP_API;
let baseUploadUrl = import.meta.env.VITE_APP_UPLOAD_API;
let iconfontVersion = ['3732372_rfhe81cj04l'];
let iconfontUrl = `//at.alicdn.com/t/c/font_$key.css`;
let codeUrl = `${window.location.origin}/code`
export {
    baseUrl,
    baseUploadUrl,
    iconfontUrl,
    iconfontVersion,
    codeUrl,
}
